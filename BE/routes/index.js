const articlesPath = require('./articles');
const mediasPath = require('./medias');
const loginPath = require('./login');
const clientsPath = require('./clients');
const usersPath = require('./users');

module.exports = {articlesPath, mediasPath, loginPath, clientsPath, usersPath};
